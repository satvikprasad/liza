var custom_print = fn (a b) {
    ret fn (val) [a b] {
        print a + " " + val + " " + b;

        a = a + 1; b = b + 1;
    };
};

var pp = custom_print (5 20);

for var i = 0; i < 200; i = i + 1 {
    pp ("satvik");
}

print pp; print custom_print;
